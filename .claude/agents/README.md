# Agents

Snapshots. The originals live in `modryn-hq@v4:.claude/agents/` — fix a character there, then
re-copy here. Nothing warns you when these fall behind.

- **`craft/`** — any product. `oprah` intake · `jobs` identity · `ogilvy` copy · `rams` design ·
  `kay` frontier scope · `charlie` strategy · `michelle` engineering · `dana` IP
- **`trading/`** — `nathan` psychology and discipline, where the wedge lives · `hollis` risk, sizing
  and prop economics · `vera` the market read in the trader's own grammar · `priya` the CFTC line,
  which is real here because `Read` tells a trader about their own trading

`soren` (macro and regime) is deliberately absent: Run has no macro surface and `NOT IN V1` adds
none.

They build. `/postcheck` judges. Never the same seat for both.
